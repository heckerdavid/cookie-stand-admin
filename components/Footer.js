export default function Footer({totalLocations}) {
    return (
        <footer className=" bg-violet-500 mt-1 p-4 mx-auto">
            <p className="mx-auto px-96">{totalLocations} Locations World Wide</p>
        </footer>
  )
}